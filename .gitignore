.obsidian

# Skill caches (fetched upstream content, not source-controlled)
skills/*/.cache/

# Rollback snapshots (contain certs/keys/secrets)
skills/*/.rollback/

# Topology env files (contain site-specific IPs, hostnames, usernames)
skills/*/topology.env
.kgm-index.jsonl
